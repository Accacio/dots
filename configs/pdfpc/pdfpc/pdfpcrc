bind J next10
bind j next
bind K prev10
bind k prev
bind G gotoLast

bind ampersand switchMode normal
bind eacute switchMode pointer
bind quotedbl switchMode pen
bind apostrophe switchMode eraser
bind parenleft switchMode spotlight
option black-on-end false
